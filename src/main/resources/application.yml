spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ekart_db
    username: postgres
    password: 6208

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
